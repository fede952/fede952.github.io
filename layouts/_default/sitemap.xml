{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Data.Pages }}
  <url>
    <loc>{{ .Permalink }}</loc>
    {{- if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>
    {{- end }}
    {{- with .Sitemap.ChangeFreq | default "weekly" }}
    <changefreq>{{ . }}</changefreq>
    {{- end }}
    {{- if or (eq .Section "cheatsheets") (eq .Section "tools") (eq .Section "debug-sys") (eq .Section "interviews") }}
    <priority>0.9</priority>
    {{- else if or (eq .Section "games") (eq .Section "guides") }}
    <priority>0.7</priority>
    {{- else }}
    <priority>{{ .Sitemap.Priority | default 0.5 }}</priority>
    {{- end }}
    {{- range .AllTranslations }}
    <xhtml:link
      rel="alternate"
      hreflang="{{ .Language.Lang }}"
      href="{{ .Permalink }}" />
    {{- end }}
  </url>
  {{- end }}
</urlset>
