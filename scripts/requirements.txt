# Python dependencies for News Fetching Script
# Install with: pip install -r requirements.txt

feedparser==6.0.11
requests==2.31.0
python-slugify==8.0.4
beautifulsoup4==4.12.3
# DeepSeek is OpenAI-compatible; we use the openai SDK with a custom base_url.
# >=1.55.0 required: earlier versions pass `proxies` to httpx, which is removed
# in httpx 0.28+ and breaks Client.__init__ with TypeError.
openai>=1.55.0
